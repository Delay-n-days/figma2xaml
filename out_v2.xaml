<UserControl x:Class="YourNamespace.Frame37"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="26" d:DesignWidth="26">

    <!-- Frame 37 容器 -->
    <Border
        CornerRadius="0"
        BorderBrush="#FF000000"
        BorderThickness="1"
        Padding="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <!-- Rectangle -->
            <Border
                Width="12"
                Height="12"
                Background="#CC6680"
                Grid.Row="0"
                Grid.Column="0"
                Margin="0,0,3,4"/>
            <!-- Rectangle -->
            <Border
                Width="12"
                Height="12"
                Background="#CC6680"
                Grid.Row="0"
                Grid.Column="1"
                Margin="3,0,0,4"/>
            <!-- Rectangle -->
            <Border
                Width="12"
                Height="12"
                Background="#CC6680"
                Grid.Row="1"
                Grid.Column="0"
                Margin="0,4,3,0"/>
        </Grid>
    </Border>
</UserControl>