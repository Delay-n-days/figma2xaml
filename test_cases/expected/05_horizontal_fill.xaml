<Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!-- FixedLabel -->
        <TextBlock
            Text="标签:"
            FontSize="14"
            Margin="0,0,10,0"
            Grid.Column="0"/>
        <!-- FillInput -->
        <Border
            Background="#F0F0F0"
            Grid.Column="1"/>
    </Grid>