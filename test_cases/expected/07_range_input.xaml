<Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <!-- 站号范围 -->
        <TextBlock
            Text="站号范围"
            FontSize="16.5"
            Foreground="#1F2937"
            Margin="0,0,17,0"
            Grid.Column="0"/>
        <!-- Input1 容器 -->
        <Border
            CornerRadius="4"
            BorderBrush="#D1D5DB"
            BorderThickness="1"
            Background="#FFFFFF"
            Padding="24,6,24,6"
            Grid.Column="1">
            <!-- 1 -->
            <TextBlock
                Text="1"
                FontSize="16.5"
                Foreground="#1F2937"
                VerticalAlignment="Center"/>
        </Border>
        <!-- — -->
        <TextBlock
            Text="—"
            FontSize="16.5"
            Foreground="#1F2937"
            Margin="0,0,17,0"
            Grid.Column="2"/>
        <!-- Input2 容器 -->
        <Border
            CornerRadius="4"
            BorderBrush="#D1D5DB"
            BorderThickness="1"
            Background="#FFFFFF"
            Padding="18,6,18,6"
            Grid.Column="3">
            <!-- 16 -->
            <TextBlock
                Text="16"
                FontSize="16.5"
                Foreground="#1F2937"
                VerticalAlignment="Center"/>
        </Border>
    </Grid>